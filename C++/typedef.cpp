#include <iostream>
#include <vector>

typedef std::vector<std::pair::string, int>> pairlist_t;

int main(){
    /* typedef = reserved keyword used to create an additional name (alias)
    for another data type. Ne identiefier for an 
    existing type. Helps with readablility and reduces typos.*/

    pairlist_t pairlist;

    return 0;
}